# دليل التشغيل الفعلي — نظام الحسابات

> حالة النظام الآن: قاعدة بيانات **فارغة نظيفة** مخططها مُنشأ بالكامل من الترحيالات.
> لا مستخدمين ولا منشآت ولا بيانات. هذا هو الوضع الصحيح لبدء الإدخال الفعلي.

---

## 1. ما يجب ضبطه قبل أول تشغيل

خمس إعدادات فقط. كلها في `appsettings.json` أو `.env` — **ولا سرّ واحد فيها يُكتب في ملف مُرفوع إلى Git**.

| #   | الإعداد                               | القيمة المطلوبة                                   | لماذا                                                                                               |
| --- | ------------------------------------- | ------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| 1   | `ApiSettings:Secret`                  | قيمة عشوائية **32 حرفاً على الأقل**، خاصة بالخادم | مفتاح توقيع رموز الدخول. التطبيق **يرفض الإقلاع** إن كان فارغاً أو أقصر من ٣٢ حرفاً                 |
| 2   | `AdminUser:Username`                  | اسم المستخدم الذي ستسجّله                         | بدونه **لا يملك أي حساب صلاحية المدير الرئيسي**، فلا يُدار سجل التدقيق ولا سعر الصرف على كل الخزائن |
| 3   | `ConnectionStrings:DefaultConnection` | سلسلة اتصال قاعدة بيانات الخادم                   | العنوان الحالي `localhost\SQLEXPRESS` محلي                                                          |
| 4   | `CorsSettings:AllowedOrigins`         | نطاق الواجهة الفعلي                               | وإلا رفض المتصفح كل نداء                                                                            |
| 5   | `MailSettings:*`                      | بيانات خادم البريد                                | مفصولة في `MAIL-SETUP.md`                                                                           |

### كيف تُضبط الأسرار على الخادم

**لا تُكتب في `appsettings.json`.** استخدم ملف `.env` بجانب التطبيق:

```env
ApiSettings__Secret=<قيمة عشوائية 64 حرفاً>
AdminUser__Username=<اسم حساب المدير>
ConnectionStrings__DefaultConnection=Server=<خادم>;Database=cash;User Id=<مستخدم>;Password=<كلمة مرور>;TrustServerCertificate=True
```

> `.env` مُستثنى من Git. الأولوية: `appsettings.json` ← `appsettings.{البيئة}.json` ← **متغيّرات البيئة** ← `.env`.

### توليد مفتاح التوقيع

مُدمَج في التطبيق:

```
dotnet run -- --generate-secret
```

يُنتج قيمة عشوائية مُشفَّرة قوية (48 بايتاً بترميز Base64) جاهزة للنسخ مباشرةً في `.env`:

```
مفتاح توقيع جديد (ضعه في .env كما هو):
ApiSettings__Secret=Bdo4GNh3uaPCkIYcfIKzC1tvc/B08uZ8ZVKUq/LlMHdJVpyOQoyQsJ4zQRhBktcb
```

> يعمل هذا الأمر **حتى على خادم لم يُضبط فيه المفتاح بعد** — وُضع قبل فحص المفتاح عمداً، فالخادم الجديد هو الحالة التي يُحتاج فيها بالضبط.
> **لا تستخدم مفتاح جهاز التطوير على الخادم.**

---

## 2. أول تشغيل — ثلاث خطوات مُختبَرة

جُرِّبت هذه الخطوات فعلياً على قاعدة بيانات فارغة، ونجحت كلها.

### أ) أنشئ أول حساب

من الواجهة، أو مباشرةً:

```
POST http://localhost:5003/API/User/register
Content-Type: application/json

{ "username": "admin", "password": "<كلمة مرور قوية>", "name": "<الاسم>", "email": "<البريد>" }
```

كلمة المرور: **٨ أحرف على الأقل، وتحتوي حرفاً ورقماً على الأقل**.

### ب) اجعله مديراً رئيسياً

اضبط `AdminUser__Username` في `.env` بنفس اسم المستخدم، **ثم أعد تشغيل التطبيق**. عند الإقلاع يمنح النظام الحساب صلاحية المدير الرئيسي تلقائياً.

> يمكن ضبط `AdminUser` **قبل** إنشاء الحساب أيضاً؛ النظام يمنح الصلاحية عند كل إقلاع إن وجد الحساب ولم يكن مديراً بعد.

### ج) أنشئ المنشأة الأولى

سجّل الدخول، ثم:

```
POST /API/Business
Authorization: Bearer <التوكن>

{ "name": "<اسم الجهة>" }
```

**من يُنشئ المنشأة يصبح مالكها `owner` تلقائياً** — وهذا مُتحقَّق منه. ثم أنشئ الخزائن من الواجهة.

---

## 3. ما تم التحقق منه فعلياً على قاعدة فارغة

| الفحص                                | النتيجة                                     |
| ------------------------------------ | ------------------------------------------- |
| التطبيق يُقلع على قاعدة بيانات فارغة | ✅                                          |
| تطبيق الترحيالات من الصفر            | ✅ **٨ ترحيالات · ١٧ جدولاً**               |
| `AuditLogs` بعد الإنشاء من الصفر     | ✅ **٢٧ عموداً · ٨ فهارس** (مطابق للتقرير)  |
| `POST /API/User/register`            | ✅ 201                                      |
| تسجيل الدخول                         | ✅ 200                                      |
| إنشاء أول منشأة                      | ✅ مُنشأة، والمنشئ **owner**                |
| إنشاء أول خزنة                       | ✅                                          |
| إنشاء أول حركة                       | ✅ 200                                      |
| الأرصدة بعد الحركة                   | ✅ `balanceIqd = 100000` · `balanceUsd = 0` |
| سجل التدقيق كتب السطور               | ✅ ١٧ سطراً                                 |
| **تسريب كلمة المرور في سجل التدقيق** | ✅ **صفر**                                  |
| أخطاء خادم أثناء التهيئة             | ✅ **صفر**                                  |
| منح المدير الرئيسي من الإعدادات      | ✅ `IsSuperAdmin = 1` بعد إعادة التشغيل     |

**النتيجة: ١٩ فحصاً ناجحاً.** الفحصان «الفاشلان» كانا توقّعاً خاطئاً في سكربت الاختبار لا خللاً في النظام — انظر البند ٥.

---

## 4. النسخ الاحتياطي — نسختان جاهزتان

قبل تفريغ قاعدة البيانات أُخذت نسختان كاملتان:

| المسار                                                                                          | ملاحظة                                          |
| ----------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| `C:\Users\PRO1\cashbook-recovery\backup\cash-before-clean.bak`                                  | **يمكنك نسخها والوصول إليها** (10.4 ميغابايت)   |
| `C:\Program Files\Microsoft SQL Server\MSSQL16.SQLEXPRESS\MSSQL\Backup\cash-before-clean-*.bak` | نسخة الخادم — الوصول إليها يحتاج صلاحيات مرتفعة |

كلتاهما مُتحقَّق من صلاحيتها بـ`RESTORE VERIFYONLY`. تحتويان بيانات الاختبار كاملة (مستخدمون ومنشآت وحركات وسجل تدقيق) — **لا حاجة إليها إلا للرجوع للخلف**.

**للاستعادة:**

```
sqlcmd -S <خادم> -E -Q "RESTORE DATABASE [cash] FROM DISK = N'<مسار الملف>.bak' WITH REPLACE;"
```

> ⚠️ **للإنتاج:** لا تعتمد على نسخة يدوية. اضبط مهمة مجدولة على الخادم تأخذ نسخة يومية، واختبر استعادتها فعلاً على بيئة تجريبية.

---

## 5. ملاحظات على رموز الاستجابة

معظم نقاط الإنشاء تُرجع **`200 OK`** لا `201 Created` — رغم أن جسم الاستجابة يقول `"statusCode": 201`.

| النقطة                    | HTTP الفعلي | `statusCode` في الجسم |
| ------------------------- | ----------- | --------------------- |
| `POST /API/User/register` | `201`       | `201`                 |
| `POST /API/Business`      | **`200`**   | `201`                 |
| `POST /API/Book`          | **`200`**   | `201`                 |
| `POST /API/Transaction`   | `200`       | `200`                 |

**لماذا يهمّك:** إن كنت تكتب اختباراً آلياً أو مراقبة تعتمد على `201`، فستفشل مع هذه النقاط. **كلها 2xx فتُعامَل كنجاح** في المتصفحات وأدوات الواجهة.

التصحيح ممكن (توحيد الرمز إلى 201) لكنه يمسّ ستة متحكّماً — قرّره إن أردت.

---

## 6. محظورات لا تُخالف

| ❌ لا تفعل                                  | ✅ افعل                                                   |
| ------------------------------------------- | --------------------------------------------------------- |
| كتابة أي سرّ في `appsettings.json`          | `.env` على الخادم أو متغيّرات البيئة                      |
| رفع ملف `.env` إلى Git                      | تحقّق: `git check-ignore -v .env`                         |
| استخدام مفتاح توقيع جهاز التطوير على الخادم | مفتاح جديد خاص بالخادم                                    |
| الاعتماد على `Swagger` في الإنتاج           | معطَّل تلقائياً خارج بيئة التطوير — استخدم `swagger.json` |
| تشغيل النظام بمستخدم `sa` لقاعدة البيانات   | حساب مخصّص بأقل صلاحية تكفي                               |

---

## 7. المتبقّي

| #   | البند                        | من ينفّذه                            |
| --- | ---------------------------- | ------------------------------------ |
| 1   | **تحويل مستودع Git إلى خاص** | أنت — الخطوات في القسم ٨             |
| 2   | ضبط خادم البريد الفعلي       | أنت — `MAIL-SETUP.md`                |
| 3   | إنشاء أول حساب ومنشأة        | أنت — القسم ٢ أعلاه                  |
| 4   | شاشات الواجهة الأربع الناقصة | مطوّر الواجهة — `API-INTEGRATION.md` |

---

## 8. خصوصية المستودع

المستودع مرفوع حالياً على GitHub **بصفة عامة**:

```
https://github.com/Dote-Team/cashbookback-v-1.0.0
```

التزام قديم `6777a47` يحتوي على مفتاح التوقيع القديم وكلمة مرور المسؤول الأول. **المفتاح دوّرناه فلم يعد صالحاً**، لكن يبقى ظاهراً في تاريخ Git لأي زائر.

### نتيجة الفحص الفعلي (15 أيلول 2026)

| الفحص                              | النتيجة                                                                           |
| ---------------------------------- | --------------------------------------------------------------------------------- |
| هل المستودع عام الآن؟              | ⚠️ **نعم** — يُفتح بدون تسجيل دخول                                                |
| هل ملف `.env` مُتعقَّب؟            | ✅ لا                                                                             |
| هل في الشجرة الحالية أي سرّ مكشوف؟ | ✅ **نظيفة تماماً**                                                               |
| أين يظهر المفتاح القديم؟           | في التزام واحد فقط: `6777a47 "Add project files."` — وقد أُزيل في الالتزام التالي |
| هل المفتاح الحالي هو المحروق؟      | ✅ **لا** — دُوِّر إلى قيمة جديدة بطول 64 حرفاً (القديم 32)                       |

**لماذا لا يُعدّ الأمر خطراً أمنياً حالياً:** القيم المكشوفة في ذلك الالتزام **ميتة كلها** —

- مفتاح التوقيع القديم: النظام يوقّع الآن بمفتاح آخر، فأي رمز دخول مُوقَّع بالمفتاح القديم **يُرفض**.
- `Jwt:Secret` القديم: لم يعد موجوداً في ملف الإعدادات ولا يقرؤه أي كود.
- كلمة مرور المسؤول الأول: حقلها أُزيل من الإعدادات، ومنح الصلاحية لا يستخدم كلمة مرور أصلاً.

**فما يبقى مكشوفاً في الحقيقة شيئان:** الشيفرة المصدرية كاملة، وقيم لم تعد صالحة. ولأن النظام نظام حسابات جهة حكومية، فالشيفرة وحدها سبب كافٍ لجعله خاصاً.

### الخطوة المطلوبة (دقيقة واحدة)

1. افتح `https://github.com/Dote-Team/cashbookback-v-1.0.0/settings`
2. انزل إلى **Danger Zone**
3. **Change repository visibility** ← **Private** ← اكتب اسم المستودع للتأكيد

### لتنظيف السجل نهائياً (اختياري، لاحقاً)

تنظيف التاريخ يعيد كتابة كل الالتزامات ويلزمه دفع قسري. لا تفعله إلا بعد التأكد من أن لا أحد آخر يعمل على المستودع. اطلب المساعدة إن أردت.

---

## 9. بيانات تجريبية جاهزة (اختيارية)

مجموعة كاملة ومترابطة لتشغيل كل شاشات الواجهة الأمامية على بيانات واقعية بدل قاعدة فارغة.
**موجودة الآن في قاعدة البيانات المحلية** — القاعدة لم تعد فارغة، انظر التحذير في آخر القسم.

### التشغيل والإزالة

```
sqlcmd -S localhost\SQLEXPRESS -d cash -E -i Seed\SeedDemoData.sql   -f 65001
sqlcmd -S localhost\SQLEXPRESS -d cash -E -i Seed\RemoveDemoData.sql -f 65001
```

> ⚠️ **`-f 65001` إلزامي.** الملفان بترميز UTF-8 وفيهما نصوص عربية، و`sqlcmd` يقرأ ملفات `-i` كـANSI بدونه فتُشوَّه الأسماء العربية.
>
> سكربت الإدخال يرفض العمل إذا كانت البيانات موجودة مسبقاً، ويطلب تشغيل سكربت الإزالة أولاً. كلاهما داخل معاملة واحدة: أي خطأ يتراجع عن كل شيء ولا يُبقى نصف بيانات.

### ما تُدخله البذور

| البند              | العدد | البند        | العدد |
| ------------------ | ----- | ------------ | ----- |
| المنشآت            | 5     | الحركات      | 2500  |
| الخزائن            | 20    | سجل الحركات  | 2860  |
| المستخدمون         | 8     | أسعار الصرف  | 1060  |
| العضويات           | 21    | الفئات       | 60    |
| الحقول المخصصة     | 40    | طرق الدفع    | 30    |
| إعدادات الخزائن    | 20    | جهات الاتصال | 60    |
| قيم الحقول المخصصة | 1228  |              |       |

**التوزيع مقصود ليكون واقعياً:** ٧٠٪ دينار و٣٠٪ دولار · ٥٥٪ إيداع و٤٥٪ سحب · الحركات موزّعة على ٣٦٤ يوماً مختلفاً وعلى ٢٠ خزنة بالتساوي (١٢٥ حركة لكل خزنة) · ١٥٪ بلا فئة و٢٠٪ بلا طريقة دفع و٣٠٪ بلا جهة اتصال — لتُختبر الحالات الفارغة في الواجهة.

### المبادئ التي بُنيت عليها

- **معرّفات ثابتة مقصودة** (`a0000000-…01..05` للمنشآت، `b0000000-…` للخزائن، `c0000000-…` للمستخدمين) فيمكن تتبّع أي صف إلى أصله، والحركات بمعرّفات عشوائية لكثرتها وتُحذف عبر `BookId`.
- **قواعد العمل مُحترَمة كما في الشيفرة لا كما في التخمين:** `Type` مُطبَّع إلى `cash in`/`cash out` · العملة نصاً `IQD`/`USD` · سعر الصرف للدولار وحده · تاريخ الصرف للإيداع بالدولار فقط والسحب بالدولار يرفضه · `BookIds` مصفوفة JSON · صف `Setting` لكل خزنة · الفهرس الفريد على `(BookId, Currency, RateDate)` محترَم.
- **مُتحقَّق آلياً:** خمسة عشر فحصاً على قواعد العمل رجعت كلها بصفر مخالفة، وفحص سلامة `BookIds` بصفر مخالفة.
- **الإزالة لا تمسّ سجل التدقيق** ولا منشآت أخرى ولا المستخدم المالك.

### الحسابات التجريبية

| المستخدم | الدور في المنشأة          |
| -------- | ------------------------- |
| `demo1`  | partner · viewer          |
| `demo2`  | partner · viewer          |
| `demo3`  | partner · staff           |
| `demo4`  | portfolio_manager · admin |
| `demo5`  | admin · privateviewer     |
| `demo6`  | dataoperator · viewer     |
| `demo7`  | partner · staff           |
| `demo8`  | portfolio_manager · staff |

كلها بنفس كلمة مرور المستخدم المالك المذكور في أعلى `Seed\SeedDemoData.sql` (الافتراضي `aaaa`)، لأن السكربت ينسخ بصمة bcrypt نفسها. تُستخدم لتجربة شاشة الصلاحيات: كل دور يرى خزائن مختلفة.

> ⚠️ **قبل الإنتاج:** شغّل `Seed\RemoveDemoData.sql`. لا تُسلّم نظاماً حقيقياً وفيه ٢٥٠٠ حركة وهمية وثمانية حسابات تجريبية. البذور للتطوير والعرض فقط.
