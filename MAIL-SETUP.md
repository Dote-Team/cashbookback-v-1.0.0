# تفعيل إرسال البريد الإلكتروني

> **الخلاصة:** النظام كان يحتفظ بإعدادات البريد لكنه **لا يرسل بريداً إطلاقاً**. أُصلح ذلك، ويبقى ضبط بيانات خادم الدائرة.

---

## 1. ما كان مكسوراً فعلاً

قبل الإصلاح، لم يكن `EmailService` قادراً على إرسال أي بريد حتى لو كانت الإعدادات صحيحة. ثلاثة خلل:

| #   | الخلل                                                                     | الأثر                                                                                     |
| --- | ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- |
| 1   | **`SendInvitationEmailAsync` بلا أي مستدعٍ**                              | دالة مكتوبة كاملة ولا ينادِيها شيء — **صفر بريد يُرسل من النظام**                         |
| 2   | **`UseSsl` مُهمَل تماماً** — الوضع مُثبَّت على `SecureSocketOptions.None` | أي خادم بريد حقيقي (Gmail · Outlook · Exchange · خادم الدائرة) **يرفض** الاتصال بلا تشفير |
| 3   | **`Email` و `Password` لا يُستخدمان** — لا استدعاء لـ`AuthenticateAsync`  | المصادقة مستحيلة، فلا يعمل أي خادم يتطلّب اسم مستخدم وكلمة مرور                           |

وبدلاً من الإرسال، كان `SendInvaiteByEmail` **يضيف العضو إلى قاعدة البيانات فقط** — ولهذا كان `POST /API/invitations` يرجع 200 في كل الاختبارات: لا شيء كان يُرسل حتى يفشل.

---

## 2. ما أصبح عليه الحال

- `MailSettings.Enabled` — مفتاح صريح. عند `false` يُتخطّى الإرسال **بلا أي محاولة اتصال**، فلا تتأخّر إضافة العضو.
- `UseSsl` صار يُحترم: **587** ← `STARTTLS` · **465** ← `SSL` مباشر · `false` ← بلا تشفير (للخوادم الداخلية فقط).
- مصادقة SMTP تعمل عند وضع `Email` و `Password`.
- `TimeoutSeconds` (افتراضياً 15) يمنع تعليق الطلب على خادم لا يستجيب.
- بريد الإشعار **بالعربية** ومُهيّأ RTL، ويذكر المنشأة والدور بوسم عربي.
- **فشل البريد لا يُلغي إضافة العضو أبداً**، وحالة الإرسال تُعاد في استجابة الطلب.

---

## 3. التشغيل — ثلاث خطوات

### أ) ضع كلمة المرور في `.env` بجانب التطبيق

لا توضع كلمة المرور في `appsettings.json` أبداً. أضف هذه الأسطر إلى ملف `.env` على خادم الدائرة:

```env
MailSettings__Enabled=true
MailSettings__SmtpServer=smtp.example.gov.iq
MailSettings__SmtpPort=587
MailSettings__UseSsl=true
MailSettings__FromName=نظام الحسابات
MailSettings__FromEmail=no-reply@example.gov.iq
MailSettings__Email=no-reply@example.gov.iq
MailSettings__Password=كلمة-مرور-الحساب-هنا
MailSettings__TimeoutSeconds=15
```

> `.env` مُستثنى من Git، فلن تُرفع كلمة المرور.

### ب) أو من إعدادات النظام على الخادم

بديل مكافئ: اضبط نفس الأسماء كمتغيّرات بيئة على مستوى نظام التشغيل. الأولوية: `appsettings.json` ← `appsettings.{البيئة}.json` ← **متغيّرات البيئة** ← `.env`.

### ج) تحقّق

أضف عضواً إلى منشأة من الواجهة، أو:

```
POST /API/invitations
{ "email": "user@example.com", "role": "staff", "businessId": "<guid>" }
```

وسترى في الاستجابة:

```json
{
  "message": "member added successfully.",
  "emailSent": true,
  "emailNote": "أُرسل إشعار البريد إلى العضو."
}
```

---

## 4. المراجع الشائعة

| الخادم                  | المنفذ    | `UseSsl`   | ملاحظة                                                                  |
| ----------------------- | --------- | ---------- | ----------------------------------------------------------------------- |
| Gmail                   | 587       | `true`     | يحتاج **كلمة مرور تطبيق** لا كلمة مرور الحساب، مع تفعيل التحقّق بخطوتين |
| Outlook / Microsoft 365 | 587       | `true`     | قد يمنع الإرسال بلا «SMTP AUTH» مُفعَّلاً للحساب                        |
| Exchange الدائرة        | 587 أو 25 | حسب الخادم | اسأل مسؤول الشبكة عن المنفذ والتشفير                                    |
| خادم داخلي بلا مصادقة   | 25        | `false`    | اترك `Email` و `Password` فارغين                                        |

---

## 5. كيف تعرف أن الإرسال فشل

**في الاستجابة** (رسالة عامة، بلا تفاصيل داخلية):

| `emailSent` | `emailNote`                                  | المعنى                                      |
| ----------- | -------------------------------------------- | ------------------------------------------- |
| `true`      | «أُرسل إشعار البريد إلى العضو.»              | نجح                                         |
| `false`     | «إرسال البريد معطَّل في إعدادات الخادم.»     | لم يُحاول — `Enabled = false`               |
| `false`     | «إعدادات خادم البريد غير مكتملة…»            | لم يُحاول — اسم الخادم أو بريد المُرسل فارغ |
| `false`     | «أُضيف العضو، لكن تعذّر إرسال إشعار البريد.» | **حاول وفشل** — راجع السجل                  |

**في سجل التطبيق** (التفصيل التقني — لا يُعاد للعميل):

```
Mail delivery failed: SocketException - No connection could be made because the target machine actively refused it.
```

> **في الحالتين العضو مُضاف فعلاً.** فشل البريد يعني أن الإشعار لم يصل، لا أن العملية فشلت.

---

## 6. نتيجة التحقّق

جُرِّبت السيناريوهات الثلاثة على النظام الحقيقي:

| السيناريو                           | رمز الاستجابة | العضو أُضيف؟ | زمن الطلب       | البريد                    |
| ----------------------------------- | ------------- | ------------ | --------------- | ------------------------- |
| البريد معطَّل (الافتراضي المُسلَّم) | `200`         | ✅           | 760 مللي ثانية  | لم يُحاول                 |
| مُفعَّل بلا خادم مُعرَّف            | `200`         | ✅           | 183 مللي ثانية  | لم يُحاول                 |
| مُفعَّل وخادم غير متاح              | `200`         | ✅           | 2242 مللي ثانية | حاول وفشل — وسُجِّل السبب |

**صفر أخطاء خادم في الحالات الثلاث.**

---

## 7. ملاحظة على معنى «الدعوة»

النظام الحالي **يضيف العضو مباشرة** عند الدعوة، ثم يُرسل بريداً يُعرّفه بعضويته ودوره. لا توجد خطوة «قبول دعوة»: لا جدول دعوات، ولا رمز دعوة، ولا نقطة قبول.

إن أردت لاحقاً تدفّقاً كاملاً (بريد يحمل رابطاً صالحاً لمدة محدودة، والعضو يقبله بنفسه) فهذا يحتاج: جدول دعوات + ترحيل + نقطة قبول + صلاحية زمنية + شاشة في الواجهة. عبّرت عن هذا في `API-INTEGRATION.md` أيضاً — القرار قرارك، والنظام يعمل بلا حاجة إليه.
